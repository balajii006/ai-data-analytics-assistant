# AI-Powered Data Analytics Assistant

Upload a CSV file and ask a business question in natural language.

The application uses:
- Pandas for CSV cleaning
- SQLite for SQL execution
- Gemini API for natural-language-to-SQL generation
- Gradio for the web interface
- Matplotlib for charts

Flow:

CSV → Pandas → SQLite → Gemini → SQL → Result → Chart → Business Insight
